# LanguageTool JVM memory settings
# For 2GB VPS: JAVA_XMX=1g
# For 4GB VPS: JAVA_XMX=2g (default)
JAVA_XMS=512m
JAVA_XMX=2g

# Cloudflare Tunnel token
# Get it from: Cloudflare Dashboard → Zero Trust → Networks → Tunnels → Create
# Then configure public hostname: lt.nest42.com → http://languagetool:8010
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here
